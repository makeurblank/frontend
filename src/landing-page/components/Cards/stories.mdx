import { Meta, Canvas, Story } from '@storybook/addon-docs';
import { useState } from 'react';

import Cards from './index';

<Meta
  title="landing-page/Cards"
  component={Cards}
/>

export const Template = (args) => <Cards {...args} />;

<Canvas>
  <Story name="cards"
    args={{ values: [{ id: 1, value: 'card1' }, { id: 2, value: 'card2' }] }}
    argTypes={{
      onClick: { action: 'click' },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
