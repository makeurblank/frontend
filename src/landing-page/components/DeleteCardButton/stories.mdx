import { Meta, Canvas, Story } from '@storybook/addon-docs';
import { useState } from 'react';

import DeleteCardButton from './index';

<Meta
  title="landing-page/DeleteCardButton"
  component={DeleteCardButton}
/>

export const Template = (args) => {
  const [value, setValue] = useState('');
  return <DeleteCardButton {...args} />
}

<Canvas>
  <Story name="button" argTypes={{ onClick: { action: 'click' } }}>
    {Template.bind({})}
  </Story>
</Canvas>
