import { Meta, Canvas, Story } from '@storybook/addon-docs';
import { useState } from 'react';

import CreateCardButton from './index';

<Meta
  title="landing-page/CreateCardButton"
  component={CreateCardButton}
/>

export const Template = (args) => <CreateCardButton {...args} />;

<Canvas>
  <Story name="button" argTypes={{ onClick: { action: 'click' } }}>
    {Template.bind({})}
  </Story>
</Canvas>
